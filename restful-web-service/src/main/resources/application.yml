server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

spring:
  messages:
    basename: messages

  security:
    user:
      name: username
      password: passwOrd

management:
  endpoints:
    web:
      exposure:
        include: "*"